// Defines the 8pt spacing scale used across the UI, so layouts stay consistent.
// This exists to keep all screens aligned to the same rhythm.
// It fits in the app by acting as the single source of spacing constants.
class AppSpacing {
  static const double xxs = 4;
  static const double xs = 8;
  static const double sm = 16;
  static const double md = 24;
  static const double lg = 32;
  static const double xl = 40;
  static const double xxl = 48;

  const AppSpacing._();
}
